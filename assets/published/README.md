# Published assets — pipeline output

**NEVER hand-edited.** Everything here is generated by `src/aifactory_twin/`. If something here
is wrong, the pipeline is wrong. Re-run it.

See DESIGN_NOTES.md ADR-08.
