# Source assets — vendor drops

**NEVER edited in place.** These files stand in for what a vendor ships. If something here is
wrong, fix it in the pipeline (`src/aifactory_twin/ingest/`) so the fix survives the next drop.

See DESIGN_NOTES.md ADR-08.
